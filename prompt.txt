Contexto:
Atuas como um Engenheiro de Software Sénior e Especialista em Regras Fiscais a trabalhar no sistema "DM Intelligence" (stack: React + NestJS). Realizámos uma auditoria ao Motor Fiscal (TCO) e identificámos 3 falhas graves de arquitetura fiscal no cálculo de impostos que precisam de ser corrigidas imediatamente.

Conforme as minhas diretrizes estritas: Nunca omitas nada na tua resposta. Escreve o código completo e refatorado para as alterações solicitadas em cada ficheiro.

Objetivo:
Corrigir a lógica de cálculo de créditos fiscais (ICMS, IPI, PIS, COFINS) no Backend e sincronizar a mesma lógica nas pré-visualizações do Frontend para garantir que o sistema não induz o utilizador em erro.

Regras Fiscais Corretas (A implementar):

Regra de Ouro (Uso e Consumo): Se itemUseType === 'CONSUMPTION', nenhum crédito é gerado (retorna 0 para todos os impostos), independentemente de qualquer regime.

ICMS: Compradores de Lucro REAL e PRESUMIDO têm direito a crédito de ICMS se o item for INDUSTRIAL_INPUT ou RESALE. (Atualmente o sistema está a bloquear o ICMS para o Presumido).

PIS/COFINS: Apenas compradores de Lucro REAL têm direito. Trava Crítica: Se o comprador for REAL mas o fornecedor for SIMPLES, o crédito de PIS/COFINS tem de ser 0.

IPI: Apenas gera crédito se o comprador for Indústria (não SIMPLES) e o item for INDUSTRIAL_INPUT.

Passos de Execução Obrigatórios:

1. Backend - Refatorar o Motor Fiscal (backend/src/modules/tax-engine/tax-credit.service.ts):

Remove a atribuição de crédito de ICMS de dentro da verificação if (buyerRegime === 'REAL'). O ICMS deve estar acessível também para o PRESUMIDO.

Adiciona a trava do Fornecedor Simples Nacional: Se supplierRegime === 'SIMPLES', então creditPis = 0 e creditCofins = 0.

Retorna um array de alerts sempre que um imposto for zerado devido ao regime (ex: "Créditos de PIS/COFINS anulados: Fornecedor Simples Nacional").

2. Frontend - Refatorar Simulação no TcoManager.tsx (components/TcoManager.tsx):

Atualiza a lógica visual para espelhar rigorosamente o Backend.

Garante que a interface não exibe simulações de crédito de PIS/COFINS se o fornecedor selecionado for do SIMPLES ou se o comprador for PRESUMIDO.

3. Frontend - Refatorar Resumos no PurchasingHub.tsx e QuoteList.tsx:

Localiza as funções calculateTco ou calculatePreview nestes ficheiros.

Substitui a lógica falha (if (buyer?.taxRegime === 'REAL') credits += icms + pis + cofins;) pela lógica completa descrita nas "Regras Fiscais Corretas" acima. Avalia o itemUseType, supplierTaxRegime e separa os impostos adequadamente.

Gera os blocos de código completos e refatorados para o tax-credit.service.ts, TcoManager.tsx, PurchasingHub.tsx e QuoteList.tsx.

Após analisares este plano, diz-me se tens alguma dúvida sobre as regras de negócio e aguarda o meu "OK" antes de começares a gerar o código.